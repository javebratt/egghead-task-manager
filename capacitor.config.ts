import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'egghead-task-manager',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
